name: firstTry

on: workflow_dispatch

jobs:
  basic-workflow:
    runs-on: ubuntu-latest

    steps:
      - name: CheckoutRepo
        uses: actions/checkout

      - name: Print environment details
        run: env